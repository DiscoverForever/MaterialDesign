<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="material.com.cn.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="material.com.cn.activity.MainActivity">

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
        <activity android:name="material.com.cn.activity.ImageListActivity"></activity>
        <activity android:name="material.com.cn.activity.GSONActivity" />
        <activity android:name="material.com.cn.activity.AnimationActivity" />
        <activity android:name="material.com.cn.activity.LeftSlideRemoveActivity" />
        <activity android:name="material.com.cn.activity.ShapeActivity"></activity>
        <activity
            android:name="material.com.cn.activity.CameraActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>